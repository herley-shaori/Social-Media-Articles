# Why Polymorphism Exists: Killing the Type Switch

This article has been published at: _<add LinkedIn / Medium URL here>_

This repository holds only the runnable code that the article references:

- [`code/v1-without-polymorphism/`](code/v1-without-polymorphism) — the switch-on-type version, including the deliberate WhatsApp validation bug.
- [`code/v2-with-polymorphism/`](code/v2-with-polymorphism) — the polymorphic version where the same bug cannot compile.
- [`code/run.sh`](code/run.sh) — compiles and runs both, plus the sanity test and the compiler-enforcement demo.
- [`images/`](images) — the before/after diagrams and the captured run output.
