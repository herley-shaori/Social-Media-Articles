%%{init: {'theme':'base','themeVariables':{'clusterBkg':'#ffffff','clusterBorder':'#8592a0','lineColor':'#5a6675','edgeLabelBackground':'#ffffff','fontFamily':'IBM Plex Sans, sans-serif'}}}%%
flowchart TB
    svc["NotificationService<br/>no switch, no enum<br/>just calls channel.deliver()"]

    ch["Channel (abstract)<br/>abstract validate()<br/>abstract format()<br/>abstract send()<br/>abstract retryDelaySeconds()"]

    svc -- "polymorphic dispatch" --> ch

    e["EmailChannel"]
    s["SmsChannel"]
    p["PushChannel"]
    w["WhatsAppChannel<br/><b>the only file added</b>"]

    e -->|extends| ch
    s -->|extends| ch
    p -->|extends| ch
    w -->|extends| ch

    note["Each subclass owns all four steps.<br/>validate() is abstract, so a new channel<br/>that forgets it does not compile."]
    w -.-> note

    classDef svc fill:#eef2f7,stroke:#38618c,color:#1f2933;
    classDef base fill:#e7f6f2,stroke:#2a9d8f,color:#1f2933;
    classDef sub fill:#f3f7fb,stroke:#38618c,color:#1f2933;
    classDef new fill:#dff3ea,stroke:#1b7a5a,color:#0f4a37;
    classDef note fill:#fff6e5,stroke:#e08a1e,color:#5b4708;
    class svc svc;
    class ch base;
    class e,s,p sub;
    class w new;
    class note note;
